define(function(require){var a=require("../println");return{println:function(){a("项目上线时，我们通常需要通过压缩合并模块文件来减少HTTP连接数，这时可以通过 map 选项来配置模块地址映射。"),a('When the project is released, we usually need to reduce HTTP connections by compressing and merging module files, then we can use "map" option to configure the mapping of modules.')}}}),define(function(require,a,b){var c=b.uri;return{text_cn:"bar 模块虽然与 foo 模块合并了，但它的 uri 属性仍与开发时保持一致："+c,text_en:'Module "bar" was merged with module "foo", but the "uri" property is still the same as it under development: '+c}});